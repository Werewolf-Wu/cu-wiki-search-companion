# 0.3.3 补丁交付验收

日期：2026-09-05。变更基于 `bc38a2d`，本记录随 0.3.3 发布提交保存。

## 变更范围

- 标题、正文、Lua 的索引、快照 handle 和准备/刷新/重建状态集中到 `PageSearchRuntime`；Data 与文件继续使用现有结构。
- 事实写入取得共享 Web Lock 后重新核验持久化版本；启动检查只读，legacy 登记和旧键清理在锁内进行，无锁不直写。
- 补充启动早点击、准备/重建并发、部分提交失败、模式隔离和旧标签版本失配等回归。
- 仅升级应用补丁版本；不改变数据库 schema、事实/快照格式、搜索算法或依赖版本。

## 自动化门禁

升版前的重构候选及升版后的 0.3.3 均通过：

| 检查 | 结果 |
| --- | --- |
| `npm test` | 36 个测试文件、341 项测试全部通过；其中新增 20 项关键回归 |
| `npm run build` | TypeScript 严格检查和 Vite 生产构建通过 |
| 版本与 userscript metadata | package、lockfile、user/meta header 均为 0.3.3；激活规则、许可证和构建标识测试通过 |
| `git diff --check` | 通过 |

## 真实浏览器

环境：Windows Edge 152.0.4191.62，复用现有测试会话，真实 Tampermonkey 安装；安装同时匹配版本与本次唯一 build marker。

升版前的相同功能代码以 0.3.2 候选运行现有快照验收脚本：

- 冷启动等待 15 秒仍为 bootstrap：1838 个标题、655 条 Data，正文/Lua/文件均未加载，快照诊断为 not-started。
- 三次快照恢复分别为 3597、2862、2777 ms，中位数 2862 ms；恢复后的六组代表性查询签名与首次构建一致。
- 清除三份可重建快照后的本地重建为 7885 ms；恢复中位数比重建快约 64%。
- 稳定正文缓存下，首次构建、三次恢复和清快照重建均为 0 个 revisions 正文请求。快照均可正常发布。

升至 0.3.3 后重新安装并验证最终版本：

| 模式 | 代表性查询 | 查询命中 / 界面结果 |
| --- | --- | --- |
| 标题 | 砖块 | 1 / 1 |
| 正文 | 砖块 | 4 / 4 |
| Lua | `_meta` | 6 / 6 |
| Data 代码 | 砖块 | 1 / 1 |
| 文件 | morphine | 1 / 1 |

最终为 jieba-wasm，索引计数为 1838 标题、1559 正文、127 Lua、655 Data、1530 文件；三类快照均 available。冒烟前后编辑器内容完全一致，Escape 正常关闭面板；未观察到 userscript 自身错误。控制台中站点既有的 CodeMirror `DEFAULTS` 与 Permissions Policy 信息不计为本脚本通过或失败的依据。

## 边界

- 未保存或提交任何 Wiki 编辑，未执行完整数据库重置；仅进行正常搜索/缓存同步和可重建快照操作。
- 本轮仅验证上述 Edge 会话，不声称完成 Firefox/Chrome 或所有多标签环境验收。普通账号验收仍沿用维护者此前的真实用户确认，不重复切号测试。
- 原始浏览器输出、机器/账号上下文及交接工作簿只在本地保留，不作为发布输入。
- 本文记录本地验收，不代表远端 CI、Nightly 或部署结果；按维护者要求不额外查询远端提交和 CI 状态。
